<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:IkerFinance.Mobile.ViewModels"
             x:Class="IkerFinance.Mobile.Views.LoginPage"
             x:DataType="viewModels:LoginViewModel"
             Title="{Binding Title}"
             Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Logo/Title Section -->
            <VerticalStackLayout Spacing="10" Margin="0,0,0,40">
                <Label Text="IkerFinance"
                       FontSize="32"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       TextColor="{StaticResource Primary}" />
                <Label Text="Manage your finances offline"
                       FontSize="14"
                       HorizontalOptions="Center"
                       Opacity="0.7" />
            </VerticalStackLayout>

            <!-- Login Form -->
            <VerticalStackLayout Spacing="15">
                <Label Text="Email" FontSize="14" />
                <Entry Text="{Binding Email}"
                       Placeholder="Enter your email"
                       Keyboard="Email"
                       ReturnType="Next" />

                <Label Text="Password" FontSize="14" Margin="0,10,0,0" />
                <Entry Text="{Binding Password}"
                       Placeholder="Enter your password"
                       IsPassword="True"
                       ReturnType="Done" />

                <HorizontalStackLayout Spacing="10" Margin="0,5,0,0">
                    <CheckBox IsChecked="{Binding RememberMe}" />
                    <Label Text="Remember me"
                           VerticalOptions="Center" />
                </HorizontalStackLayout>
            </VerticalStackLayout>

            <!-- Error Message -->
            <Label Text="{Binding ErrorMessage}"
                   IsVisible="{Binding HasError}"
                   TextColor="Red"
                   FontSize="14"
                   HorizontalOptions="Center" />

            <!-- Login Button -->
            <Button Text="Login"
                    Command="{Binding LoginCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                    Margin="0,10,0,0"
                    HeightRequest="50" />

            <!-- Loading Indicator -->
            <ActivityIndicator IsRunning="{Binding IsBusy}"
                              IsVisible="{Binding IsBusy}"
                              Color="{StaticResource Primary}" />

            <!-- Register Link -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,20,0,0">
                <Label Text="Don't have an account?"
                       VerticalOptions="Center" />
                <Label Text="Register"
                       TextColor="{StaticResource Primary}"
                       FontAttributes="Bold"
                       VerticalOptions="Center">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding NavigateToRegisterCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
